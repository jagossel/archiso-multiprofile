# archiso-multiprofile
A series of scripts that builds an offline installer with multiple profiles using archiso.

## Dependencies
The following packages will need to be installed in order to build the ISO image:
- archiso
- pacman-contrib
